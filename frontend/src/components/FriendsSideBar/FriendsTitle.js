import React from "react";

const FriendsTitle = ({ title }) => {
  return (
    <div className="uppercase text-gray-500 text-sm mt-2.5">
      {title}
    </div>
  );
};

export default FriendsTitle;
